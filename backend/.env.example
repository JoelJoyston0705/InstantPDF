# Backend Environment Variables - Example
# Copy this to .env and update values

# JWT Secret - Random string for signing authentication tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or: openssl rand -hex 32
# IMPORTANT: Change this to a random string before deploying!
JWT_SECRET=CHANGE_THIS_TO_RANDOM_STRING_MIN_32_CHARACTERS_BEFORE_DEPLOY

# Database URL - Where your data is stored
# Development: sqlite:///./instantpdf.db (local file)
# Production: postgresql://user:pass@host/dbname
DATABASE_URL=sqlite:///./instantpdf.db

# Allowed Origins - Which domains can access your API (CORS)
# Development: http://localhost:5173
# Production: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175

# Upload Settings
MAX_FILE_SIZE_MB=50
FILE_RETENTION_HOURS=24

# Email Settings (for password reset functionality)
# For Gmail: Use an App Password, not your regular password
# Generate at: https://myaccount.google.com/apppasswords
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:5173
